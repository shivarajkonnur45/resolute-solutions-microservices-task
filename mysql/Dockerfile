# mysql/Dockerfile
FROM mysql:8.0

# Copy the .env file (optional, if you need to set env vars here)
# COPY .env /etc/mysql/.env

# No need to expose ports here, Docker Compose handles that
# EXPOSE 3306

# You can add custom configurations here if needed
# COPY my.cnf /etc/mysql/conf.d/

# Healthcheck is already defined in docker-compose.yml
